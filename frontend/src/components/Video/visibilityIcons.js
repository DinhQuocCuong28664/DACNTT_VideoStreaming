import { MdOutlinePublic, MdLink, MdLockOutline } from 'react-icons/md';

/** Biểu tượng của ba chế độ hiển thị, dùng ở bộ chọn và nhãn trên thẻ video. */
export const VISIBILITY_ICON = {
  public: MdOutlinePublic,
  unlisted: MdLink,
  private: MdLockOutline,
};
